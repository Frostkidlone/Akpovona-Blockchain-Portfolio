{
  "name": "payverse-smartcontracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.12.0",
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "ethers": "^5.7.0",
    "dotenv": "^16.0.0"
  }
}